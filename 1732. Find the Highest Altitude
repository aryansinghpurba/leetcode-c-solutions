#include <stdio.h>

int largestAltitude(int* gain, int gainSize) {
    int current = 0, highest = 0;

    for (int i = 0; i < gainSize; i++) {
        current += gain[i];
        if (current > highest) {
            highest = current;
        }
    }

    return highest;
}
